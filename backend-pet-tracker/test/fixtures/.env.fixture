# Fixture usada solo por src/config/config.module.spec.ts (R5) para probar
# que ConfigModule.forRoot({isGlobal:true, envFilePath}) carga variables de
# un archivo .env sin tocar el .env real de la raíz del repo.
FIXTURE_ONLY_VAR=hello-from-fixture
