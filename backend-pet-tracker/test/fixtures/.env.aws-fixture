# Fixture usada solo por src/aws/aws.module.spec.ts (R1) para probar que
# AwsModule construye los 4 clientes AWS SDK v3 desde ConfigService, sin
# tocar el .env real de la raíz ni el fixture de src/config/config.module.spec.ts.
AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
