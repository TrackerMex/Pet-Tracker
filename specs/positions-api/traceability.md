---
feature: "positions-api"
status: approved   # draft | spec_ready (pendiente gate humano) | approved
tags: [harness, spec]
---

# Trazabilidad — [[positions-api]]

| Requisito | Test (archivo::nombre) | Commit (hash + mensaje) |
|---|---|---|
| R1 | `src/modules/positions/infrastructure/positions.controller.spec.ts::R1: ambas rutas se protegen con PetAccessGuard y sin @RequirePetRole` + `test/positions.e2e-spec.ts::R1: ambas rutas las autoriza PetAccessGuard, sin mecanismo propio` | `24db036` feat(positions-api): positions controller and module reusing PetAccessGuard (R1,R2) · e2e `d862b62` test(positions-api): e2e over postgres and localstack for both routes |
| R2 | `src/modules/positions/infrastructure/positions.controller.spec.ts::R2: la mascota consultada sale solo de request.petMembership` | `24db036` feat(positions-api): positions controller and module reusing PetAccessGuard (R1,R2) |
| R3 | `src/modules/positions/application/use-cases/get-last-position.use-case.spec.ts::R3: GET .../positions/last devuelve exactamente las 6 claves de la cache, sin DynamoDB` + `test/positions.e2e-spec.ts::R3: last devuelve las 6 claves de la cache con staleSeconds calculado` | `b1cc940` feat(positions-api): last position use case reading only the pets cache (R3,R5) · e2e `d862b62` |
| R4 | `src/modules/positions/domain/stale-seconds.spec.ts::R4: staleSeconds = max(0, floor((now - ts) / 1000)) con reloj inyectado` | `4d0d5bf` feat(positions-api): staleSeconds pure function with injected clock (R4) |
| R5 | `src/modules/positions/application/use-cases/get-last-position.use-case.spec.ts::R5: last_position NULL o corrupta => null (200 con body null), con warn en el caso corrupto` + `src/modules/positions/infrastructure/positions.controller.spec.ts::R5: last responde el body JSON null literal cuando no hay cache` + `test/positions.e2e-spec.ts::R5: sin cache, last es 200 con body JSON null (no 404, no 204)` | `b1cc940` feat(positions-api): last position use case reading only the pets cache (R3,R5) · controller `24db036` · e2e `d862b62` |
| R6 | Evidencia manual de la cadena real (simulador → poller → SQS → consumidor → Postgres) en `progress/impl_positions-api.md` §"Evidencia manual — R6": `http_status: 200`, `staleSeconds: 47` (< 120), `lat: 19.426436441538392` / `lng: -99.11776646817343`, consulta 2026-08-02T20:35:47.689Z. No aplica test automatizado (depende del reloj y de crons reales) | Verificado sobre `d862b62` test(positions-api): e2e over postgres and localstack for both routes; evidencia registrada en el commit de cierre `docs(positions-api): traceability, R6 evidence and R16 verification (R6,R16)` |
| R7 | `src/modules/positions/application/dto/list-positions.dto.spec.ts::R7: la query string se valida con zod estricto (4 opcionales, nada mas)` + `src/modules/positions/infrastructure/positions.controller.spec.ts::R7: la query string invalida es 400 antes de tocar el caso de uso` + `test/positions.e2e-spec.ts::R7: la query string invalida es 400` | `01c6de6` feat(positions-api): strict zod schema for the history query string (R7) · controller `24db036` · e2e `d862b62` |
| R8 | `src/modules/positions/application/use-cases/list-positions.use-case.spec.ts::R8: defaults to = now y from = to - DEFAULT_RANGE_MINUTES, con reloj inyectado` | `c922e85` feat(positions-api): history range defaults and limit validation (R8,R9) |
| R9 | `src/modules/positions/application/use-cases/list-positions.use-case.spec.ts::R9: from >= to es INVALID_RANGE y > 24 h es RANGE_TOO_LARGE, ambos sin consultar DynamoDB` + `test/positions.e2e-spec.ts::R9: rangos invalidos son 400 con su codigo` | `c922e85` feat(positions-api): history range defaults and limit validation (R8,R9) · e2e `d862b62` |
| R10 | `src/modules/positions/infrastructure/repositories/position-history.dynamo.reader.spec.ts::R10: una sola Query por pagina, pk = PET#<petId>, sk BETWEEN inclusive, ascendente y Limit fijo` + `test/positions.e2e-spec.ts::R10: la Query respeta el rango inclusive y el orden ascendente` | `4a68b23` feat(positions-api): dynamodb history reader and public item mapper (R10,R11) · e2e `d862b62` |
| R11 | `src/modules/positions/infrastructure/mappers/position-response.mapper.spec.ts::R11: el mapper traduce el item de DynamoDB a camelCase y omite los atributos internos` + `src/modules/positions/application/use-cases/list-positions.use-case.spec.ts::R11: el envelope tiene exactamente items y nextCursor` + `test/positions.e2e-spec.ts::R11: envelope {items, nextCursor} y items sin atributos internos` | `4a68b23` feat(positions-api): dynamodb history reader and public item mapper (R10,R11) · envelope `ae7e0ef` feat(positions-api): low_accuracy filter and query-driven pagination (R11,R12,R15) · e2e `d862b62` |
| R12 | `src/modules/positions/application/use-cases/list-positions.use-case.spec.ts::R12: por defecto se ocultan solo los low_accuracy; includeSuspect=true no filtra nada` + `test/positions.e2e-spec.ts::R12: por defecto se ocultan solo los low_accuracy` | `ae7e0ef` feat(positions-api): low_accuracy filter and query-driven pagination (R11,R12,R15) · e2e `d862b62` |
| R13 | `src/modules/positions/domain/cursor.spec.ts::R13: el cursor es base64url opaco {v, p, q, k} y hace round-trip exacto` + `src/modules/positions/application/use-cases/list-positions.use-case.spec.ts::R13: nextCursor sale del LastEvaluatedKey y reanuda la lectura en el sk codificado` + `test/positions.e2e-spec.ts::R13: paginar siguiendo nextCursor recorre la serie completa sin huecos ni duplicados` | `db61572` feat(positions-api): opaque base64url cursor codec and domain errors (R13,R14) · use case `c3cc018` feat(positions-api): cursor emission and cross-pet rejection in the list use case (R13,R14) · e2e `d862b62` |
| R14 | `src/modules/positions/domain/cursor.spec.ts::R14: un cursor que no decodifica, no es v=1 o esta incompleto es InvalidCursorError` + `src/modules/positions/application/use-cases/list-positions.use-case.spec.ts::R14: cursor corrupto, de otra mascota o de otra consulta => InvalidCursorError sin Query` + `test/positions.e2e-spec.ts::R14: un cursor manipulado o de otra mascota es 400 INVALID_CURSOR y no filtra nada` | `db61572` feat(positions-api): opaque base64url cursor codec and domain errors (R13,R14) · use case `c3cc018` feat(positions-api): cursor emission and cross-pet rejection in the list use case (R13,R14) · e2e `d862b62` |
| R15 | `src/modules/positions/application/use-cases/list-positions.use-case.spec.ts::R15: la paginacion es de la Query, no del resultado filtrado` + `test/positions.e2e-spec.ts::R15: la paginacion es de la Query, no del resultado filtrado` | `ae7e0ef` feat(positions-api): low_accuracy filter and query-driven pagination (R11,R12,R15) · e2e `d862b62` |
| R16 | Verificación por inspección del diff en `progress/impl_positions-api.md` §"Verificación de no regresión — R16": `git diff main --stat` (33 archivos, solo `src/modules/positions/**`, `src/app.module.ts` (+2 líneas), `test/positions.e2e-spec.ts`, `specs/**`, `progress/**` y `feature_list.json` de bookkeeping), cero migraciones, cero cambios en `src/db/**`, `src/workers/**`, `src/pipeline/**`, `src/modules/pets/**`, `package.json` ni `.env.example`; `./init.sh` verde (482 unit en 77 suites) y `pnpm run test:e2e` verde (84 en 7 suites). No aplica test automatizado | Verificado sobre `d862b62` test(positions-api): e2e over postgres and localstack for both routes; registrado en el commit de cierre `docs(positions-api): traceability, R6 evidence and R16 verification (R6,R16)` |

Regla: el reviewer no aprueba si alguna fila queda "pendiente".
Convención de commit: `feat(<scope>): <desc> (R1,R2)`.
El implementer actualiza esta tabla tras cada commit; el reviewer la valida
al aprobar (ver [[../../docs/specs|specs]] y [[../../CHECKPOINTS|CHECKPOINTS]] C5).
