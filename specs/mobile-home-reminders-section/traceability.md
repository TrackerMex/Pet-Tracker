---
feature: "mobile-home-reminders-section"
status: approved       # draft | spec_ready | approved
tags: [harness, spec]
---

# Trazabilidad — [[mobile-home-reminders-section]]

Rutas relativas a `mobile-pet-tracker/`. La columna **Test** la **prescribe la
spec** (no la improvisa el implementer): es el contrato de qué prueba cada R-id.
La columna **Commit** la rellena el implementer en cuanto ese requisito queda
verde, nunca al final.

Tres abreviaturas para no repetir rutas largas:
`HOME` = `src/screens/home/index.test.tsx`;
`SEC` = `HOME`::`describe('#70 R1: la Home dibuja la sección de recordatorios')`;
`FMT` = `src/screens/home/format.test.ts`.

| Requisito | Test (archivo::nombre) — prescrito por la spec | Commit (hash + mensaje) |
|---|---|---|
| R1 | `SEC`::`describe('#70 R1: estructura de la sección')`::`it('dibuja la cabecera y el cuerpo de la sección')` (dos hijos directos de `reminders-section`, rótulo con su receta, enlace presente). Por la decisión humana D1, la cardinalidad vive en R9, cuando existen sus tres hijos reales | `f498f1e feat(mobile-home-reminders-section): add section header and body (R1)` + `0e00251 feat(mobile-home-reminders-section): render bound vaccine row (R1,R6)` |
| R2 | `SEC`::`it('tipa nextVaccine con los tres campos del contrato y ninguno más')`, lectura de `src/api/types.ts`: `id`/`name`/`nextDoseAt` presentes, `daysLeft` y `date` ausentes, `nextReminder` y `activitySummary` **todavía `unknown`**; más `tsc --noEmit` de R19, que es lo que prueba que el tipo compila contra el uso de R6. **Candado de cadena literal**, declarado como tal en [[requirements]] R17 | `df759b8 feat(mobile-home-reminders-section): type next vaccine contract (R2)` |
| R3 | `SEC`::`describe('#70 R3: la barra de comidas queda fuera')`::`it('no dibuja la barra de comidas ni pide el plan de nutrición')`: un solo hijo directo en el cuerpo con el perfil cargado, ningún nodo con texto de la forma `n/m` dentro de la sección, y ningún import de `../../api/nutrition`. Se cierra con la mutación **M5** —segundo hijo **sin `testID`**— versionada en el rojo | `264c490 feat(mobile-home-reminders-section): keep meals row out of scope (R3)` |
| R4 | `FMT`::`describe('#70 R4: calendarDaysUntil cuenta días de calendario')`::`it('cuenta futuro, mañana, hoy y pasado')`: `'2026-09-15'`→`5`, `'2026-09-11'`→`1`, `'2026-09-10'`→`0`, `'2026-09-08'`→`-2`, todos desde `new Date(2026, 8, 10, 12, 0)` | `3badfec feat(mobile-home-reminders-section): count normalized calendar days (R4)` |
| R5 | `FMT`::`describe('#70 R5: la zona horaria no desplaza fechas')` → `it('normaliza ambos días por componentes sin parsear la cadena cruda')`: los dos horarios del día 10 y un `now` sintético con día local 10/día UTC 11 devuelven `5`; el espía de `global.Date` no recibe ninguna construcción, `Date.parse` no recibe llamadas y `Date.UTC` recibe exactamente los componentes de objetivo/hoy locales en los tres casos. Más `it('formatea la fecha visible sin desplazarla')`, cuyo espía de constructor recibe exactamente `[[2026, 8, 15]]` y nunca la cadena cruda. D5 reemplaza el `process.env.TZ` inerte y D7 cierra el lado de `now` con candados independientes de la zona del runner | implementación original `a6b82bf` + `af8ae4e`; candados D5 `5385ed8 feat(mobile-home-reminders-section): harden date and row locks (R5,R6,R8,R19b)` + `238e414 feat(mobile-home-reminders-section): guard raw Date construction (R5,R19b)`; candado D7 `232c38b feat(mobile-home-reminders-section): lock local day and row shape (R5,R8,R12)` |
| R6 | `SEC`::`describe('#70 R6: datos de la próxima vacuna')`::`it('liga nombre, fecha y contador a su nodo y a ninguno más')`, con `within(getByTestId('reminders-next-vaccine'))` sobre la fixture `{ id: 'vac-9', name: 'Antirrábica', nextDoseAt: '2026-09-15' }`: los tres datos exactos, ninguno cruzado y el id ausente; por D6 observa además `icon-syringe` **dentro** de la fila, su tinta `--color-category-blue-strong` y las recetas exactas de nombre y fecha. Más `it('no hace pulsable la fila de la vacuna')` | `0e00251 feat(mobile-home-reminders-section): render bound vaccine row (R1,R6)` + `5385ed8 feat(mobile-home-reminders-section): harden date and row locks (R5,R6,R8,R19b)` |
| R7 | `SEC`::`describe('#70 R7: ramas del contador de vacuna')`::`it('resuelve las tres ramas del contador')`, tres renders con `jest.setSystemTime`: `5 d`/`Faltan 5 días`, `Hoy`, `Vencida`; y en ninguna el texto del contador contiene `'-'` | `675390c feat(mobile-home-reminders-section): resolve countdown branches (R7)` |
| R8 | `SEC`::`describe('#70 R8: estado vacío de próxima vacuna')`::`it('dibuja un estado vacío con forma de fila cuando no hay vacuna')`: `reminders-none-upcoming` con su texto, `reminders-next-vaccine` y los tres `testID` de R6 ausentes, rótulo y enlace presentes; por D6 observa además `icon-syringe` **dentro** de la fila, su tinta `--color-muted` y la receta exacta del texto vacío; por D7 exige `flex-row items-center gap-3` para conservar la anatomía horizontal | `0752baf feat(mobile-home-reminders-section): render empty vaccine state (R8)` + `5385ed8 feat(mobile-home-reminders-section): harden date and row locks (R5,R6,R8,R19b)` + `232c38b feat(mobile-home-reminders-section): lock local day and row shape (R5,R8,R12)` |
| R9 | `SEC`::`describe('#70 R9: estados de carga y error del detalle')`::`it('esqueletiza mientras carga y calla cuando el perfil falla')`: esqueleto con su `className` en `pending()`, cuerpo con **cero** hijos en `{ kind: 'error' }`, ningún `testID` de sección terminado en `-error` o `-retry`, y `reminders-see-all` presente en los dos escenarios + `it('deja el cuerpo con un solo hijo')`, trasladado íntegro por D1: cuenta **`getByTestId('reminders-section-body').children`** —nunca coincidencias de `testID`— y espera `1` cargado, `1` con esqueleto y `0` en error. Es el candado que mata M5 | `36012d4 feat(mobile-home-reminders-section): render detail loading skeleton (R9)` |
| R10 | `SEC`::`describe('#70 R10: enlace a la lista de recordatorios')`::`it('lleva a la lista de recordatorios existente')` (`mockRouter.push` **una vez** con `'/reminders'`, cruzado con los ficheros reales de `src/app/(tabs)/` vía el helper `appRoutes`, `HOME:42-57`) + `it('no añade un segundo camino a la lista desde la Home')` (`'/reminders'` **una sola vez** en el fuente; sin `as Href`; sin import de `Href`). El candado de #71 (`HOME:1601-1612`, que prohíbe `'/reminders'` dentro de `QUICK_ACTIONS`) queda **verde y sin tocar**. Tras autorización humana, `it('muestra feedback visual al pulsar el enlace')` fija además C8 | `0844b75 feat(mobile-home-reminders-section): link reminders list (R10)` + `ed457ba feat(mobile-home-reminders-section): add pressed link feedback (R10)` |
| R11 | `SEC`::`describe('#70 R11: accesibilidad por partes')`::`it('anuncia el enlace como botón y expande la abreviatura del contador')`: `accessibilityRole` `'button'` solo en `reminders-see-all`, `accessibilityLabel` del contador en las tres ramas, y ni `reminders-section` ni `reminders-section-body` con `accessible` o `accessibilityLabel`. Candado sobre código correcto → rojo por **mutación de producción** (quitar el `accessibilityLabel`), nunca por mock | `e34b07e feat(mobile-home-reminders-section): restore accessible countdown label (R11)` |
| R12 | `SEC`::`describe('#70 R12: Card compartido y tokens')`::`it('viste la sección con el Card compartido y los tokens')`: `className` del contador (fondo y tinta categóricos resueltos), `className` del disco, `TABULAR_NUMS` en el contador y ausente en nombre y fecha; por D7, el segundo hijo de la fila cargada conserva el envoltorio `flex-1` de nombre + fecha. Más, **en verde y sin tocar**, `consistency-classnames.test.ts` #64 R9 (`:404`, `:430`), #62 R4 (`:150-155`) y #62 R1 (`:97-104`); y los deltas declarados de #62 R15 y #61 R4 (R18). Se cierra con **M8** y la sonda O5-b | `677ab51 feat(mobile-home-reminders-section): restore tabular countdown numerals (R12)` + `232c38b feat(mobile-home-reminders-section): lock local day and row shape (R5,R8,R12)` |
| R13 | `SEC`::`describe('#70 R13: icono de vacuna')`::`it('usa el icono de reicon y ningún emoji')`, lectura de fuente: import de `Syringe`, dos usos con `size={20}`, ausencia de `💉`. La entrada `Syringe: mockIcon('icon-syringe')` del doble (`HOME:82-102`) es **preparación de test y no cuenta como rojo**; no renombra ni elimina ninguna de las seis existentes. **Candado de cadena literal**, declarado como tal en [[requirements]] R17 | `285efa7 feat(mobile-home-reminders-section): restore syringe icons (R13)` |
| R14 | `SEC`::`describe('#70 R14: posición y condición de la sección')`::`it('coloca la sección entre la actividad semanal y la última posición')`, que filtra los hijos de `home-content` a la lista blanca de **seis** y espera `['summary-card','collar-card','quick-actions','weekly-activity-card','reminders-section','last-position-card']` + `it('no dibuja la sección sin mascota seleccionada')` + los tres candados de orden heredados, `HOME:1205-1235` (#68), `HOME:1401-1438` (#69) y `HOME:1760-1798` (#71), **en verde y sin tocar**. Se cierra con **M7** | `2666977 feat(mobile-home-reminders-section): place section after weekly activity (R14)` |
| R15 | `SEC`::`describe('#70 R15: sin llamadas nuevas')`::`it('no añade ninguna llamada a la API')`, que compara el recuento de `mockGetPet`, `mockGetDailyActivity` y `mockListPets` con el escenario equivalente. Verificación adicional del reviewer: `git diff --stat` sin ficheros fuera de `mobile-pet-tracker/`, `specs/` y `progress/`, y en particular **cero** en `backend-pet-tracker/`, `src/utils/reminder-dates.ts`, `src/components/`, `src/theme/` y `src/app/` | `1ad0d08 feat(mobile-home-reminders-section): preserve existing API call count (R15)` |
| R16 | `src/__tests__/ui-language.test.ts`::`describe('#70 R16: copy de recordatorios de la Home')` + los candados de #65 (una clave presente en un idioma y ausente en el otro no compila; `checkUses(R3_HOME)` exige una fila por uso) + las **siete** filas nuevas en `ui-copy-table.ts` + el registro en `specs/mobile-ui-language/design.md` §2. Ninguna clave del ámbito `reminders.*` se reutiliza; `reminders.dueInDays` es además inutilizable aquí por su `·` incorporado | `b5575f9 feat(mobile-home-reminders-section): register reminder copy (R16)` |
| R17 | `src/__tests__/design-drift.test.ts`::`describe('#70 R17: la sección de recordatorios no mete drift de estilo')`, sobre la lista nominal de los seis ficheros de esta feature —`src/api/types.ts` **no está en ninguna de las cinco listas existentes**, así que sin este bloque quedaría sin vigilar—. Título sin cifra escrita con letra. **Candado de cadena literal, no de conducta**, y así está declarado en [[requirements]] R17 | `f81668c feat(mobile-home-reminders-section): restore tokenized icon color (R17)` |
| R18 | Los deltas de la tabla de [[requirements]] R18, verificados por el reviewer **rehaciendo cada `grep`**, no leyendo el informe del implementer. Fila 1: `src/providers/__tests__/language-provider.test.tsx:41`, `260 + 16 + 1 + 4` → `260 + 16 + 1 + 4 + 7` — el candado que **paró la implementación en #68 y en #69**. Fila 3: **tres expresiones** de `consistency-classnames.test.ts` que solo cuadran juntas (constante `HOME_TABULAR_DELTA_70`, fila de home y total cerrado) más la guarda de `#69 R14`. Fila 4: `legibility-classnames.test.ts` `1 → 2` y `.toBe(13)` → `.toBe(13 + 1)`. Esta feature **no reubica nada** | `dcd0faa feat(mobile-home-reminders-section): apply declared inventory deltas (R18)` |
| R19 | Requisito de **verificación** (C4 vía (b)): `bun run test` y `bun run typecheck` verdes desde `mobile-pet-tracker/`, grep-clean de la carta §Decisiones fijas 3 intacto y escala de radios de #62 R4 sin clase fuera de escala | `d22e1c9 feat(mobile-home-reminders-section): restore tabular numerals after M8 (R19)` + `87ecd82 feat(mobile-home-reminders-section): record mutation evidence (R19)` + `c93aa24 feat(mobile-home-reminders-section): record final gate (R19)` + `35f8c8d feat(mobile-home-reminders-section): record second-pass final gate (R19)` |
| R19b | Las **nueve** mutaciones de [[tasks]] §R19 (2) más D7, plantadas de una en una y **todas en código de producción** (`CHECKPOINTS.md` C4, quinto punto: mutar el doble de `reicon` o `makePet` **no** cuenta), con la evidencia en `progress/impl_mobile-home-reminders-section.md`. Por D5, M1 y M2 ponen roja la suite completa con `bun run test`, `TZ` sin definir: 1 fallo/1077 verdes cada una; por D7, M9 produce el mismo rojo sobre el lado local de `now`; M5 planta el hijo intruso **sin `testID`** | M1 final `544a525`/`a50245e`; M2 final `75dd5c1`/`bcde085`; M3 `58338f3`/`1c940e8`; M4 `6e9206d`/`06cd7cf`; M5 `1763391`/`c13b934`; M6 `ca88209`/`91d636d`; M7 `bffaa47`/`a223d4c`; M8 `efb9bba`/`d22e1c9`; M9 `9adea68`/`4140c2c` — cada par restaura exactamente el fichero de producción |

Regla: el reviewer no aprueba si alguna fila queda "pendiente" en la columna
Commit.
Convención de commit: `feat(mobile-home-reminders-section): <desc> (R1,R6)`.
El implementer actualiza esta tabla tras cada commit; el reviewer la valida al
aprobar (ver [[../../docs/specs|specs]] y [[../../CHECKPOINTS|CHECKPOINTS]] C5).

## Gates humanos, no delegables

Dos, y ninguno lo puede cerrar una IA:

1. **Aprobación de la spec** ([[requirements]] §Aprobación), con las seis
   ratificaciones de esa sección — en particular que esta feature entrega
   **media sección** del diseño y que la causa por la que la otra mitad no entra
   **no es** la que decía el enunciado.
2. **Prueba de humo en dev build de Android**, en los **dos temas**, con:
   - una mascota **con** vacuna próxima (fila completa: icono, nombre, fecha
     localizada y contador en `N d`);
   - una mascota **sin** vacuna próxima (estado vacío de R8, con la sección
     conservando su altura);
   - el enlace **"Ver todos"** pulsado, comprobando que abre la lista de
     recordatorios y que el botón "Nuevo" de esa pantalla sigue funcionando;
   - el tile **"Recordatorio"** de #71 pulsado en la misma sesión, comprobando
     que lleva al **alta** y no a la lista — que es la distinción que R10
     defiende.
